# PIPD Eval Framework – Python Dependencies
# Install: pip install -r requirements.txt --break-system-packages

# ── Core data manipulation ────────────────────────────────────────────────────
pandas>=2.0.0              # CSV/DataFrame loading, filtering, groupby, export
PyYAML>=6.0                # eval_config/pipd_eval_config.yaml for eval report weights/thresholds

# ── String matching ───────────────────────────────────────────────────────────
python-Levenshtein>=0.21   # Fast C-extension edit-distance for near-miss detection

# ── API framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0           # REST API routing, request/response validation
uvicorn[standard]>=0.29.0  # ASGI server to run FastAPI
pydantic>=2.0.0            # Request body validation (bundled with FastAPI >=0.110)
python-dotenv>=1.0.0       # Load OPENAI_API_KEY from .env for AI reports
python-docx>=1.1.0         # Export composite / AI reports as .docx
matplotlib>=3.7.0          # Optional PNG charts for eval reports (skipped if not installed)

# ── Standard library (no install required) ───────────────────────────────────
# json      – JSON serialisation / deserialisation
# os        – Environment variable access
# pathlib   – Cross-platform file path handling
# argparse  – CLI argument parsing
# datetime  – Timestamping eval runs
# typing    – Type annotations
# sys       – Exit codes, path manipulation
# time      – Performance timing for batch runner
# collections – Counter for failure type aggregation
